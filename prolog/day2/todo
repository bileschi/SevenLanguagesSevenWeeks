find
 an implementation of the fibonacci sequence
 -- fib(0, 1).
 -- fib(1, 1).
 -- 
 -- fib(X, Y):-
 -- 	X > 1, X1 is X - 1, X2 is X - 2, fib(X1, Z), fib(X2, W), Y is W + Z.

 an implementation of the towers of hannoi problem
 -- http://www.csupomona.edu/~jrfisher/www/prolog_tutorial/pt_framer.html

do
 reverse the elements of a list
 return the smallest element of a list
 sort a list